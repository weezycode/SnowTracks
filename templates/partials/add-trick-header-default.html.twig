<header class="masthead " style="background-image: url(' {{ asset('images/figure/default-image.jpg') }} ' )">
    <ul class="nav justify-content-end navTrick">
        <li class="nav-item">
            <a href="#"><button type="button" class="btn ">
                    <i class="bi bi-pencil-square icon"></i></a>
        </li>
        <li class="nav-item">
            <form action="#" method="post"
                onsubmit="return confirm('Vous ne pouvez supprimer cette image, ajoutez en pour ne plus l\'avoir ! ');">
                <input type="hidden" name="token" value="{{ csrf_token('delete') }}" />
                <button type="submit" class="btn">
                    <i class="bi bi-trash icon"></i>
                </button>
            </form>
        </li>
    </ul>
    <div class="container position-relative px-4 px-lg-5">
        <div class="row gx-4 gx-lg-5 justify-content-center">
            <div class="col-md-10 col-lg-8 col-xl-7">
                <div class="site-heading">
                    <h1 class="fw-bolder mb-1 text-center">{{ trick.name }} </h1>
                </div>
            </div>
        </div>
    </div>
</header>